

```bash
git init 
git status

# git repository  ( github, gitlab , bitbucket )
# 1. web UI -> create repo 
# 2. cli -> create repo 

git remote -v
git remote show origin 

# to create remote repo 
gh repo create sample-project-git-evening --public 

# master ( store the latest version )
# git branching strategies 
git add . 
git commit -m "" 

git commit -am ""

git branch development 

```